%
% Resume
% Preamble
%

\documentclass[a4paper,10pt]{article}

% Document margins
\usepackage[
  top=1in,
  right=0.6in,
  bottom=1in,
  left=0.5in
]{geometry}

% Headers
% use fancyhdr to style page headers.
\usepackage{fancyhdr}
\pagestyle{fancy}
% disable header ruler
\renewcommand{\headrulewidth}{0pt}

% Sections
% disable section numbering
\setcounter{secnumdepth}{0}
% lower fontsize
\usepackage[compact]{titlesec}

% Links
% style links the same way as the rest of the text
\usepackage[colorlinks=true]{hyperref}
\urlstyle{same}

% Font 
\usepackage[default]{sourcesanspro}

% Lists
% disable spacing between list items
\usepackage{enumitem}
\setlist{nolistsep} 

% Custom Commands
% \experience{title}{organisation}{start date}{end date}
% Generate a experience sub-section header.
\NewDocumentCommand{\experience}{mmmm}{\subsection{#1 \textnormal{#2 \hfill #3 - #4}}}

% \project{title}{url}
% Generate a project sub-section header.
% url: do not append protocol to url (eg. https://)
\NewDocumentCommand{\project}{mm}{\subsection{#1 \hfill \textnormal{\small{\href{https://#2}{#2}}}}}

\title{Zhu Zhanyan's Resume}
\begin{document}
